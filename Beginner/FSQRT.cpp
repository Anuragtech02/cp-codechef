#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    while (n--)
    {
        int number;
        cin >> number;
        cout << (int)sqrt(number) << endl;
    }
}